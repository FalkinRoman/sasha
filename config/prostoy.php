<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Режим предпродажи
    |--------------------------------------------------------------------------
    | PRESALE_MANUAL_PAYMENT=true: заказ создаётся в статусе «ожидает оплаты»,
    | доступ к курсу выдаётся только после подтверждения админом (или ЮKassa при отключённом ручном режиме).
    */
    'presale_mode' => env('PRESALE_MODE', true),

    'presale_manual_payment' => env('PRESALE_MANUAL_PAYMENT', true),

    /** Промокод из БД, который автоматически сравнивается при расчёте (берётся лучшая скидка). */
    'presale_auto_promo_code' => env('PRESALE_AUTO_PROMO_CODE', 'PRESALE20'),

];
