# Скопируй в .env для Docker: cp .env.docker.example .env
# Потом: docker compose up -d --build

APP_NAME=ProstoYoga
APP_ENV=production
APP_KEY=
# После key:generate смени .env и пересоздай app, иначе в контейнере останется старый пустой APP_KEY:
# docker compose up -d --force-recreate app
APP_DEBUG=false
# Локально: тот же хост:порт, что в HTTP_PORT (ниже). 127.0.0.1 — меньше конфликтов с другим софтом на localhost.
APP_URL=http://127.0.0.1:8888

LOG_CHANNEL=stack
LOG_LEVEL=warning

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=prostoyoga
DB_USERNAME=prostoyoga
DB_PASSWORD=придумай_сложный_пароль

# Пароль root для контейнера MariaDB (обязателен для docker-compose)
DB_ROOT_PASSWORD=другой_сложный_пароль_root

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_DOMAIN=
CACHE_STORE=database
QUEUE_CONNECTION=database
FILESYSTEM_DISK=local

# Порты на твоём Mac → в nginx внутри всё равно 80/443. На проде в .env выставь 80/443 или как у хостинга.
HTTP_PORT=8888
HTTPS_PORT=8443
# default.conf — только HTTP (локалка). На VPS с TLS: production.conf + серты в /etc/letsencrypt
NGINX_CONF_FILE=default.conf
MYSQL_PORT=3306

# Остальное скопируй из .env.example (почта, ЮKassa, Telegram, LESSON_VIDEO_MAX_MB, …)
